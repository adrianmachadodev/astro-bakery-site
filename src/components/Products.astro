---
import { PRODUCTS } from "../const/products";
import CardProducts from "./CardProducts.astro";

console.log("products", PRODUCTS);
---

<section
  id="products"
  class="h-auto p-3 flex flex-row flex-wrap items-center gap-5 justify-around scroll-mt-36"
  data-aos="fade-up"
>
  <h2 class="w-full text-center text-yellow-600 mt-4">
    NUESTRAS TORTAS MÁS ELEGIDAS
  </h2>
  {
    PRODUCTS.map((product: any) => (
      <CardProducts
        name={product.name}
        description={product.description}
        image={product.image}
      />
    ))
  }
</section>

